import { SecondsminutesPipe } from './secondsminutes.pipe';

describe('SecondsminutesPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsminutesPipe();
    expect(pipe).toBeTruthy();
  });
});
